<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= menu.name %> を編集</title>
</head>
<body>
    <h1>メニュー編集 (ID: <%= menu.id %>)</h1>
    <h2><%= menu.name %> の編集フォーム</h2>

    <!-- 更新処理のPOSTエンドポイントに送信 -->
    <form action="/menu/<%= menu.id %>" method="POST">
        
        <div>
            <label for="name">店名</label>
            <input type="text" id="name" name="name" value="<%= menu.name %>" required>
        </div>

        <div>
            <label for="menu">メニュー名</label>
            <input type="text" id="menu" name="menu" value="<%= menu.menu %>" required>
        </div>


        <div>
            <label for="price">金額 (¥)</label>
            <input type="number" id="price" name="price" value="<%= menu.price %>" required min="0">
        </div>

        <div>
            <label for="note">備考</label>
            <textarea id="note" name="note" rows="4"><%= menu.note %></textarea>
        </div>

        <button type="submit">内容を更新する</button>
        <a href="/menu/<%= menu.id %>">キャンセルして詳細に戻る</a>
    </form>

</body>
</html>